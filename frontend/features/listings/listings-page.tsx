import { PlaceholderPage } from '@/features/shared/placeholder-page';

type ListingsPageProps = {
  title: string;
};

export function ListingsPage({ title }: ListingsPageProps) {
  return <PlaceholderPage title={title} />;
}
